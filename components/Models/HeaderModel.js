export const options = ['Home', 'Shop', 'Contact', 'My Account'];

export const accountMenuItems = ['Orders', 'Settings', 'My Wallet', 'Address', 'Notifications'];

export function handleOptionPress(option) {
  // Handle actions when an option is pressed
  console.log('Clicked on:', option);
}

export function handleAccountMenuItemPress(accountMenuItems) {
  // Handle actions when an account menu item is pressed
  console.log('Clicked on:', menuItem);
}
